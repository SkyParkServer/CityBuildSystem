const:
  prefix: "&6&lSkyPark &8»"
  no-perm: "&7Du hast dazu keine Rechte!"
  default-spawn-cooldown-seconds: 5
  default-home-limit: 1

# These were Skript vars; we keep defaults here, but actual persisted values live in globals.yml
spawn:
  cooldown-seconds: 5

debug:
  enabled: false

mysql:
  host: "127.0.0.1"
  port: 3306
  database: "citybuild"
  user: "root"
  password: "password"
  useSSL: false
server-name: "server-a"
